services:
  # Open WebUI - Chat interface that connects to the combined Ghidra+MCP container
  # On first start, auto-creates an admin user (WEBUI_ADMIN_EMAIL/PASSWORD),
  # then auto-discovers all MCP tool servers and binds them to the default model
  # so every new chat has tools available without manual setup.
  # OFFLINE_MODE suppresses the "What's New" modal on login; inference & MCP still work.
  open-webui:
    image: ghcr.io/open-webui/open-webui:0.9.6
    ports:
      - "80:8080"
    environment:
      - OPENAI_API_BASE_URL=${BASE_URL}
      - OPENAI_API_KEY=${API_KEY}
      - ENABLE_OLLAMA_API=False
      - ENABLE_OPENAI_API=True
      - BYPASS_MODEL_ACCESS_CONTROL=True
      - ENABLE_DIRECT_CONNECTIONS=True
      - OFFLINE_MODE=True
      - ENABLE_VERSION_UPDATE_CHECK=False
      - ENABLE_PERSISTENT_CONFIG=False
      - DEFAULT_MODELS=${DEFAULT_MODEL}
      - DEFAULT_MODEL_PARAMS={"function_calling":"native"}
      - WEBUI_ADMIN_EMAIL=${WEBUI_ADMIN_EMAIL:-demo@demo.demo}
      - WEBUI_ADMIN_PASSWORD=${WEBUI_ADMIN_PASSWORD:-demo}
      - TOOL_SERVER_CONNECTIONS=[{"type":"mcp","url":"http://ghidra-mcp-headless:8090/mcp","spec_type":"url","spec":"","path":"openapi.json","auth_type":"none","key":"","config":{"enable":true},"info":{"id":"ghidra","name":"ghidra-mcp","description":"Ghidra binary analysis MCP server (249 tools)"}}]
    depends_on:
      ghidra-mcp-headless:
        condition: service_started
    volumes:
      - open-webui-data:/app/backend/data
    restart: unless-stopped
    # Override CMD to bootstrap tool binding after startup, then hand off to uvicorn.
    # Runs start.sh in background → signs in as admin → discovers all MCP tools →
    # creates/updates a workspace model with every tool ID attached →
    # future MCP servers added to TOOL_SERVER_CONNECTIONS are auto-discovered.
    command:
      - /bin/bash
      - -c
      - |
        set -euo pipefail

        # Start Open WebUI in the background (start.sh → exec uvicorn)
        echo "Starting Open WebUI..."
        bash start.sh &
        WEBUI_PID=$$!

        # Wait for health check
        echo "Waiting for server to be ready..."
        for i in $$(seq 1 120); do
          if curl -sf http://localhost:$${PORT:-8080}/health > /dev/null 2>&1; then
            echo "Server healthy after $${i}s"
            break
          fi
          sleep 2
        done

        # Wait for the MCP bridge on the Ghidra container to be ready
        MCP_URL=http://ghidra-mcp-headless:8090/mcp
        echo "Waiting for MCP bridge at $${MCP_URL}..."
        for i in $$(seq 1 60); do
          if curl -sf "$${MCP_URL}" -X POST -H "Content-Type: application/json" \
            -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' > /dev/null 2>&1; then
            echo "MCP bridge ready after $${i}s"
            break
          fi
          sleep 2
        done

        # Bootstrap: sign in, discover MCP tools, bind to the default model
        python3 -c "
        import json, os, urllib.request, sys

        BASE = 'http://localhost:{}'.format(int(os.environ.get('PORT', '8080')))
        ADMIN_EMAIL = os.environ.get('WEBUI_ADMIN_EMAIL', '')
        ADMIN_PW = os.environ.get('WEBUI_ADMIN_PASSWORD', '')
        DEFAULT_MODEL = os.environ.get('DEFAULT_MODELS', 'grok-4.3')

        if not ADMIN_EMAIL or not ADMIN_PW:
            print('SKIP: WEBUI_ADMIN_EMAIL/PASSWORD not set')
            sys.exit(0)

        # Sign in as admin
        req = urllib.request.Request(
            BASE + '/api/v1/auths/signin',
            data=json.dumps({'email': ADMIN_EMAIL, 'password': ADMIN_PW}).encode(),
            headers={'Content-Type': 'application/json'},
        )
        try:
            resp = json.loads(urllib.request.urlopen(req).read())
            token = resp['token']
            print('Signed in as admin')
        except Exception as e:
            print(f'WARN: Sign in failed: {e} — tools will need manual setup')
            sys.exit(0)

        auth_hdr = {'Authorization': f'Bearer {token}', 'Content-Type': 'application/json'}

        # Discover ALL MCP tool servers (auto-detects future additions)
        req = urllib.request.Request(BASE + '/api/v1/tools/', headers={'Authorization': f'Bearer {token}'})
        try:
            tools = json.loads(urllib.request.urlopen(req).read())
            mcp_ids = [t['id'] for t in tools if t['id'].startswith('server:mcp:')]
            print(f'Discovered {len(mcp_ids)} MCP tool(s): {mcp_ids}')
        except Exception as e:
            print(f'WARN: Tool discovery failed: {e}')
            mcp_ids = []

        if not mcp_ids:
            print('SKIP: No MCP tools discovered')
            sys.exit(0)

        # Get the upstream model's capabilities (so override preserves them)
        # GET /api/models returns {'data': [...]} with all models
        caps = {}
        try:
            req = urllib.request.Request(BASE + '/api/models', headers={'Authorization': f'Bearer {token}'})
            body = json.loads(urllib.request.urlopen(req).read())
            models = body.get('data', [])
            model_data = next((m for m in models if m.get('id') == DEFAULT_MODEL), None)
            if model_data:
                caps = model_data.get('info', {}).get('meta', {}, {}).get('capabilities', {})
                print(f'Model \"{DEFAULT_MODEL}\" found with capabilities: {list(caps.keys())}')
        except Exception as e:
            print(f'WARN: Model discovery failed: {e}')

        # Create/update a workspace model with all tool IDs
        # Routes are at /api/v1/models/ (models router) not /api/models/
        payload = json.dumps({
            'id': DEFAULT_MODEL,
            'base_model_id': None,
            'name': DEFAULT_MODEL,
            'meta': {
                'toolIds': mcp_ids,
                'capabilities': caps,
                'description': 'Auto-bound to {} MCP tool server(s)'.format(len(mcp_ids)),
            },
            'params': {'function_calling': 'native'},
            'is_active': True,
        })

        # Try create first (first run: model doesn't exist in workspace yet)
        try:
            req = urllib.request.Request(
                BASE + '/api/v1/models/create',
                data=payload.encode(), headers=auth_hdr,
            )
            resp = json.loads(urllib.request.urlopen(req).read())
            print('Model created: {} — tools: {}'.format(
                resp.get('id'), resp.get('meta', {}).get('toolIds', [])))
        except urllib.error.HTTPError as e:
            err = e.read().decode()
            print(f'Create failed (HTTP {e.code}): {err[:200]}')
            if e.code == 401:
                # Model ID already taken -> update instead
                try:
                    req = urllib.request.Request(
                        BASE + '/api/v1/models/model/update',
                        data=payload.encode(), headers=auth_hdr,
                    )
                    resp = json.loads(urllib.request.urlopen(req).read())
                    print('Model updated: {} — tools: {}'.format(
                        resp.get('id'), resp.get('meta', {}).get('toolIds', [])))
                except Exception as e2:
                    print(f'WARN: Model update failed: {e2}')
            else:
                print(f'WARN: Model create failed (HTTP {e.code}), tools will need manual setup')
        except Exception as e:
            print(f'WARN: Model create failed: {e}')

        print('Bootstrap complete.')
        "

        # Hand off to the server process
        echo "Entering server loop..."
        wait $$WEBUI_PID

  # ghidra-mcp-headless - Combined Ghidra 12.1 headless REST API + Python MCP bridge
  # Merges the upstream GhidraMCP headless server (bethington/ghidra-mcp v5.13.1)
  # and its bridge_mcp_ghidra.py into a single container. The Java process serves
  # 195+ REST endpoints for binary analysis on port 8089; the Python process
  # translates them to 249 MCP tools on port 8090.
  ghidra-mcp-headless:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "${GHIDRA_MCP_HEADLESS_PORT:-3334}:8089"   # REST API (direct access for debugging)
      - "${GHIDRA_MCP_BRIDGE_PORT:-3335}:8090"      # MCP Streamable HTTP endpoint
    volumes:
      - ./binaries:/binaries:ro
      - ghidra-projects:/projects
    environment:
      - GHIDRA_MCP_PORT=8089
      - GHIDRA_MCP_AUTH_TOKEN=${GHIDRA_MCP_AUTH_TOKEN:-changeme}
      - JAVA_OPTS=-Xmx4g -XX:+UseG1GC
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8089/check_connection"]
      interval: 30s
      timeout: 10s
      start_period: 120s
      retries: 5

volumes:
  open-webui-data:
  ghidra-projects:
